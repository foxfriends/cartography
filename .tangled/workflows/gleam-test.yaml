when:
  - event: ["push"]
    branch: ["*"]

engine: "nixery"

clone:
  skip: false
  depth: 1
  submodules: false

dependencies:
  nixpkgs:
    - gleam
    - rebar3

steps:
  command: |
    cd server
    gleam test
