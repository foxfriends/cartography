when:
  - event: ["push"]
    branch: ["*"]

engine: "nixery"

clone:
  skip: false
  depth: 1
  submodules: false

dependencies:
  nixpkgs:
    - gleam

steps:
  - name: "Gleam Check"
    command: |
      cd server
      gleam check
