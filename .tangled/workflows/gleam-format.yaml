when:
  - event: ["push"]
    branch: ["*"]

engine: "nixery"

clone:
  skip: false
  depth: 1
  submodules: false

dependencies:
  nixpkgs:
    - gleam

steps:
  - name: "Gleam format"
    command: |
      cd server
      gleam format --check
